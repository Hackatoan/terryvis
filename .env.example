# Copy this file to .env and fill in your bot token
# Required
DISCORD_TOKEN=YOUR_DISCORD_BOT_TOKEN_HERE

# Optional defaults
# If set, clips default to this channel unless overridden by -setclip per server
CLIPS_CHANNEL_ID=
# Port for the web UI (defaults to 3000)
PORT=3000
# ASR engine: use 'vosk' for faster, lightweight local ASR
ASR_ENGINE=vosk
# ASR server URL (defaults to http://localhost:5005/transcribe)
# Keep `WHISPER_URL` env name for compatibility with older configs if present
WHISPER_URL=
# Vosk model path (when ASR_ENGINE=vosk)
VOSK_MODEL=./models/vosk-model-small-en-us-0.15
# To use the medium model instead, set:
# VOSK_MODEL=./models/vosk-model-en-us-0.22
# Optional advanced tuning (may require larger models / more CPU):
# VOSK_BEAM: increase to widen beam search (higher -> more accurate but slower)
# VOSK_MAX_ACTIVE: increase to allow larger search lattice (higher -> more accurate but slower)
# Example (optional):
# VOSK_BEAM=15
# VOSK_MAX_ACTIVE=7000
# Minimum mean-absolute energy required to accept a trigger (0..1). Raise to reduce false triggers.
# TRIGGER_MIN_ENERGY=0.02
# Public base URL for serving clips (used when sending clip folder links). If empty, defaults to http://localhost:3000
CLIPS_BASE_URL=
