# Copy this file to .env and fill in your bot token
# Required
DISCORD_TOKEN=YOUR_DISCORD_BOT_TOKEN_HERE

# Optional defaults
# If set, clips default to this channel unless overridden by -setclip per server
CLIPS_CHANNEL_ID=
# Port for the web UI (defaults to 3000)
PORT=3000
# ASR engine: 'whisper' (default) or 'vosk'
ASR_ENGINE=whisper
# Whisper server URL (defaults to http://localhost:5005/transcribe)
WHISPER_URL=
# Whisper model name for whisper_server.py (defaults to tiny.en)
WHISPER_MODEL=
# Vosk model path (when ASR_ENGINE=vosk)
VOSK_MODEL=./models/vosk-model-small-en-us-0.15
# Public base URL for serving clips (used when sending clip folder links). If empty, defaults to http://localhost:3000
CLIPS_BASE_URL=
